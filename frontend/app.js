const state={token:null,user:null,data:null};
const $=s=>document.querySelector(s);
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const api=(path,options={})=>{options.headers={...(options.headers||{}),...(state.token?{Authorization:`Bearer ${state.token}`}:{})};return fetch(path,options).then(async r=>{const body=await r.json().catch(()=>({}));if(!r.ok)throw new Error(body.error||`HTTP ${r.status}`);return body;});};
const post=(path,body)=>api(path,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body||{})});
const put=(path,body)=>api(path,{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify(body||{})});
const del=path=>api(path,{method:'DELETE'});
function notify(message){const n=$('#notice');if(n){n.textContent=message;n.classList.add('show');setTimeout(()=>n.classList.remove('show'),3000);}else alert(message);}
function fail(e){notify(e instanceof Error?e.message:String(e));}
function nav(){return `<nav>${[['player','🎵 Player'],['playlists','📋 Playlists'],['connections','🔌 Verbindungen'],['system','🖥 System'],...(state.user.role==='admin'?[['admin','⚙ Admin']]:[])].map(([id,label])=>`<button class="navbtn" data-tab="${id}">${label}</button>`).join('')}<button id="logout" class="danger">Abmelden</button></nav>`;}
async function load(){state.data=await api('/api/state');}
async function render(){try{await load();const d=state.data;document.body.dataset.theme=d.settings.theme||'dark';$('#app').innerHTML=`<header class="top"><div><h1>MusikBot187</h1><small>Angemeldet als ${esc(state.user.name)} · ${esc(state.user.role)}</small></div></header>${nav()}<div id="notice"></div><main id="view"></main>`;document.querySelectorAll('.navbtn').forEach(b=>b.onclick=()=>showTab(b.dataset.tab));$('#logout').onclick=()=>{state.token=null;state.user=null;renderLogin();};showTab('player');}catch(e){state.token=null;state.user=null;renderLogin();}}
async function showTab(tab){try{if(tab==='player') return playerTab();if(tab==='playlists') return playlistsTab();if(tab==='connections') return connectionsTab();if(tab==='system') return systemTab();if(tab==='admin') return adminTab();}catch(e){fail(e);}}
function playerTab(){const d=state.data;$('#view').innerHTML=`<section><h2>Suche & Wiedergabe</h2><div class="row"><input id="query" placeholder="Titel, Interpret oder Radiosender"><select id="source"><option value="all">Alle Quellen</option><option value="youtube">YouTube</option><option value="radio">Radio</option><option value="spotify">Spotify</option></select><button id="search">Suchen</button></div><div id="results" class="results"></div></section><section><div class="sectionhead"><h2>Player</h2><button id="refresh">↻ Aktualisieren</button></div><div class="now"><strong>${esc(d.current?.title||'Nichts läuft')}</strong><span>${d.paused?'Pausiert':'Bereit'} · ${d.volume}% · ${esc(d.mode)}</span></div><div class="controls"><button data-action="pause">⏸ Pause</button><button data-action="resume">▶ Weiter</button><button data-action="skip">⏭ Skip</button><button data-action="stop" class="danger">⏹ Stop</button><button data-action="clear">🗑 Queue leeren</button></div><div class="row"><label>Lautstärke <input id="volume" type="range" min="0" max="100" value="${d.volume}"></label><output id="volout">${d.volume}%</output><select id="mode"><option value="queue">Queue</option><option value="repeat">Repeat</option><option value="shuffle">Shuffle</option></select><button id="setmode">Modus speichern</button></div></section><section><div class="sectionhead"><h2>Warteschlange</h2><span>${d.queue.length} Titel</span></div><div id="queue" class="list">${d.queue.length?d.queue.map((x,i)=>`<div class="listrow"><span><b>${i+1}.</b> ${esc(x.title||x.url)}</span><button data-remove="${i}" class="danger">Entfernen</button></div>`).join(''):'<p class="muted">Queue ist leer.</p>'}</div></section>`;$('#mode').value=d.mode;$('#volume').oninput=e=>$('#volout').textContent=`${e.target.value}%`;$('#refresh').onclick=()=>showTab('player');$('#search').onclick=search;$('#query').onkeydown=e=>{if(e.key==='Enter')search();};document.querySelectorAll('[data-action]').forEach(b=>b.onclick=async()=>{try{await post(`/api/play/${b.dataset.action}`);await load();showTab('player');}catch(e){fail(e);}});$('#volume').onchange=async e=>{try{await post('/api/play/volume',{value:Number(e.target.value)});await load();}catch(x){fail(x);}};$('#setmode').onclick=async()=>{try{await post('/api/play/mode',{mode:$('#mode').value});await load();showTab('player');}catch(e){fail(e);}};document.querySelectorAll('[data-remove]').forEach(b=>b.onclick=async()=>{try{await del(`/api/queue/${b.dataset.remove}`);await load();showTab('player');}catch(e){fail(e);}});}
async function search(){const q=$('#query').value.trim();if(!q)return;$('#results').innerHTML='<p class="muted">Suche läuft …</p>';try{const r=await api(`/api/search?q=${encodeURIComponent(q)}&source=${encodeURIComponent($('#source').value)}`);const all=[...r.youtube,...r.radio,...r.spotify];$('#results').innerHTML=all.length?all.map((x,i)=>`<div class="result"><div><b>${esc(x.title)}</b><small>${esc(x.artist||x.source||'')} ${x.duration?`· ${esc(x.duration)}`:''}</small></div><button data-play-result="${i}">▶ Abspielen</button></div>`).join(''):'<p class="muted">Keine Ergebnisse.</p>';window.__results=all;document.querySelectorAll('[data-play-result]').forEach(b=>b.onclick=async()=>{try{const x=window.__results[Number(b.dataset.playResult)];await post('/api/play',{items:[{id:crypto.randomUUID(),title:x.title,url:x.url,source:x.source}]});await load();showTab('player');}catch(e){fail(e);}});}catch(e){fail(e);$('#results').innerHTML='';}}
async function playlistsTab(){const ps=await api('/api/playlists');$('#view').innerHTML=`<section><div class="sectionhead"><h2>Playlists</h2><button id="newpl">＋ Playlist</button></div><div id="pls">${ps.length?ps.map(p=>`<div class="card"><div class="sectionhead"><b>${esc(p.name)}</b><span>${p.items.length} Titel</span></div><div class="list">${p.items.map(x=>`<div class="listrow"><span>${esc(x.title||x.url)}</span><button data-premove="${esc(p.id)}" data-item="${esc(x.id)}" class="danger">Entfernen</button></div>`).join('')||'<p class="muted">Leer.</p>'}</div><div class="controls"><button data-pplay="${esc(p.id)}">▶ Ganze Playlist abspielen</button></div></div>`).join(''):'<p class="muted">Noch keine Playlists.</p>'}</div></section>`;$('#newpl').onclick=async()=>{const name=prompt('Playlist-Name');if(!name?.trim())return;try{await post('/api/playlists',{name});await showTab('playlists');}catch(e){fail(e);}};document.querySelectorAll('[data-pplay]').forEach(b=>b.onclick=async()=>{try{await post(`/api/playlists/${b.dataset.pplay}/play`);await showTab('player');}catch(e){fail(e);}});document.querySelectorAll('[data-premove]').forEach(b=>b.onclick=async()=>{try{await del(`/api/playlists/${b.dataset.premove}/items/${b.dataset.item}`);await showTab('playlists');}catch(e){fail(e);}});}
async function connectionsTab(){const [discord,ts3]=await Promise.all([api('/api/discord'),api('/api/ts3')]);$('#view').innerHTML=`<section><h2>Discord</h2><div class="card"><h3>Instanz hinzufügen / bearbeiten</h3><div class="grid"><input id="did" placeholder="ID (leer = neu)"><input id="dname" placeholder="Name"><input id="dtoken" type="password" placeholder="Bot Token"><input id="dcid" placeholder="Client-ID"><input id="dgid" placeholder="Guild-ID"><input id="dchid" placeholder="Voice Channel-ID"><input id="dprefix" value="!"><label><input id="den" type="checkbox" checked> Aktiv</label></div><button id="dsave">Discord speichern</button></div><div id="discordlist">${discord.map(x=>`<div class="card"><b>${esc(x.name)}</b><small>ID ${esc(x.id)} · ${x.enabled?'aktiv':'inaktiv'}</small><div class="controls"><button data-dconnect="${esc(x.id)}">Verbinden</button><button data-djoin="${esc(x.id)}">Voice beitreten</button><button data-ddisconnect="${esc(x.id)}">Trennen</button><button data-ddelete="${esc(x.id)}" class="danger">Löschen</button></div></div>`).join('')||'<p class="muted">Keine Discord-Instanz.</p>'}</div></section><section><h2>TeamSpeak 3</h2><div class="card"><h3>TS3-Instanz</h3><div class="grid"><input id="tid" placeholder="ID (leer = neu)"><input id="tname" placeholder="Name"><input id="thost" placeholder="Host"><input id="tport" value="9987" placeholder="Port"><input id="tnick" placeholder="Nickname"><input id="tpass" type="password" placeholder="Serverpasswort"><input id="tchannel" placeholder="Standard-Channel"><input id="ten" type="checkbox"><label><input id="ten2" type="checkbox"> Aktiv</label></div><button id="tsave">TS3 speichern</button></div><div id="ts3list">${ts3.map(x=>`<div class="card"><b>${esc(x.name)}</b><small>${esc(x.host)}:${esc(x.port)} · ${x.connected?'verbunden':'getrennt'}</small><div class="controls"><button data-tconnect="${esc(x.id)}">Verbinden</button><button data-tdisconnect="${esc(x.id)}">Trennen</button></div></div>`).join('')||'<p class="muted">Keine TS3-Instanz.</p>'}</div></section>`;$('#dsave').onclick=async()=>{try{await post('/api/discord',{id:$('#did').value,name:$('#dname').value,token:$('#dtoken').value,clientId:$('#dcid').value,guildId:$('#dgid').value,channelId:$('#dchid').value,prefix:$('#dprefix').value,enabled:$('#den').checked});await showTab('connections');}catch(e){fail(e);}};$('#tsave').onclick=async()=>{try{const id=$('#tid').value||crypto.randomUUID();await put('/api/ts3',{instances:[...ts3.filter(x=>x.id!==id),{id,name:$('#tname').value||'TS3',host:$('#thost').value,port:Number($('#tport').value||9987),nickname:$('#tnick').value||'MusikBot187',password:$('#tpass').value,channel:$('#tchannel').value,enabled:$('#ten2').checked}]});await showTab('connections');}catch(e){fail(e);}};document.querySelectorAll('[data-dconnect]').forEach(b=>b.onclick=async()=>{try{await post(`/api/discord/${b.dataset.dconnect}/connect`);notify('Discord verbunden');}catch(e){fail(e);}});document.querySelectorAll('[data-djoin]').forEach(b=>b.onclick=async()=>{try{await post(`/api/discord/${b.dataset.djoin}/join`);notify('Voice-Verbindung hergestellt');}catch(e){fail(e);}});document.querySelectorAll('[data-ddisconnect]').forEach(b=>b.onclick=async()=>{try{await post(`/api/discord/${b.dataset.ddisconnect}/disconnect`);notify('Discord getrennt');}catch(e){fail(e);}});document.querySelectorAll('[data-ddelete]').forEach(b=>b.onclick=async()=>{if(!confirm('Discord-Instanz löschen?'))return;try{await del(`/api/discord/${b.dataset.ddelete}`);await showTab('connections');}catch(e){fail(e);}});document.querySelectorAll('[data-tconnect]').forEach(b=>b.onclick=async()=>{try{await post(`/api/ts3/${b.dataset.tconnect}/connect`);notify('TS3 verbunden');await showTab('connections');}catch(e){fail(e);}});document.querySelectorAll('[data-tdisconnect]').forEach(b=>b.onclick=async()=>{try{await post(`/api/ts3/${b.dataset.tdisconnect}/disconnect`);notify('TS3 getrennt');await showTab('connections');}catch(e){fail(e);}});}
async function systemTab(){const [sys,net,storage,files]=await Promise.all([api('/api/system'),api('/api/network'),api('/api/storage'),api('/api/files')]);$('#view').innerHTML=`<section><h2>System</h2><div class="stats"><div><b>Hostname</b><span>${esc(sys.hostname||'-')}</span></div><div><b>Plattform</b><span>${esc(sys.platform||'-')}</span></div><div><b>Uptime</b><span>${esc(sys.uptime||'-')}</span></div><div><b>Node</b><span>${esc(sys.node||'-')}</span></div></div></section><section><h2>Netzwerk</h2><pre>${esc(JSON.stringify(net,null,2))}</pre></section><section><h2>Speicher</h2><pre>${esc(JSON.stringify(storage,null,2))}</pre></section><section><h2>Dateien</h2><div class="list">${files.map(x=>`<div class="listrow"><span>${x.directory?'📁':'🎵'} ${esc(x.name)}</span><small>${esc(x.path)}</small></div>`).join('')||'<p class="muted">Keine Dateien.</p>'}</div></section>`;}
async function adminTab(){const [settings,users,diag]=await Promise.all([api('/api/state'),api('/api/users'),api('/api/diagnostics')]);$('#view').innerHTML=`<section><h2>Einstellungen</h2><div class="grid"><label>Lautstärke<input id="avol" type="number" min="0" max="100" value="${settings.settings.volume}"></label><label>Modus<select id="amode"><option>queue</option><option>repeat</option><option>shuffle</option></select></label><label>Ausgabe<select id="atype"><option value="none">Keine</option><option value="discord">Discord</option><option value="ts3">TS3</option></select></label><input id="aid" placeholder="Ausgabe-ID" value="${esc(settings.settings.outputId)}"><input id="anet" placeholder="Netzwerk-Interface" value="${esc(settings.settings.networkInterface)}"><input id="adir" placeholder="Musik-Verzeichnis" value="${esc(settings.settings.filesDirectory)}"><select id="atheme"><option value="dark">Dark</option><option value="light">Light</option></select></div><button id="asave">Einstellungen speichern</button></section><section><h2>Benutzer</h2><div class="grid"><input id="uname" placeholder="Name"><input id="upass" type="password" placeholder="Passwort"><select id="urole"><option value="user">User</option><option value="admin">Admin</option></select></div><button id="uadd">Benutzer anlegen</button><div class="list">${users.map(u=>`<div class="listrow"><span>${esc(u.name)}</span><small>${esc(u.role)}</small></div>`).join('')}</div></section><section><h2>Spotify</h2><div class="grid"><input id="spid" placeholder="Client-ID"><input id="spsc" type="password" placeholder="Client-Secret"></div><button id="spsave">Spotify speichern</button></section><section><h2>Diagnose</h2><div class="diagnostics">${diag.map(x=>`<div><small>${esc(x.time)}</small><pre>${esc(x.message)}</pre></div>`).join('')||'<p class="muted">Keine Diagnosemeldungen.</p>'}</div></section><section><h2>Bot-Steuerung</h2><div class="controls"><button data-control="restart-bot">Bot neu starten</button><button data-control="stop-bot" class="danger">Bot stoppen</button><button data-control="restart-system">System neu starten</button><button data-control="shutdown-system" class="danger">System herunterfahren</button></div></section>`;$('#amode').value=settings.settings.mode;$('#atype').value=settings.settings.outputType;$('#atheme').value=settings.settings.theme||'dark';$('#asave').onclick=async()=>{try{await put('/api/settings',{volume:Number($('#avol').value),mode:$('#amode').value,outputType:$('#atype').value,outputId:$('#aid').value,networkInterface:$('#anet').value,filesDirectory:$('#adir').value,theme:$('#atheme').value});notify('Einstellungen gespeichert');await render();}catch(e){fail(e);}};$('#uadd').onclick=async()=>{try{await post('/api/users',{name:$('#uname').value,password:$('#upass').value,role:$('#urole').value});await showTab('admin');}catch(e){fail(e);}};$('#spsave').onclick=async()=>{try{await put('/api/integration/spotify',{clientId:$('#spid').value,clientSecret:$('#spsc').value});notify('Spotify gespeichert');}catch(e){fail(e);}};document.querySelectorAll('[data-control]').forEach(b=>b.onclick=async()=>{if(!confirm(`${b.textContent}?`))return;try{await post('/api/control',{action:b.dataset.control});notify('Befehl gesendet');}catch(e){fail(e);}});}
function renderSetup(){$('#app').innerHTML='<main class="auth"><h1>MusikBot187</h1><p>Ersteinrichtung</p><form id="setup"><input name="name" placeholder="Benutzername" required><input name="password" type="password" minlength="5" placeholder="Passwort (min. 5 Zeichen)" required><button>Einrichten</button></form></main>';$('#setup').onsubmit=async e=>{e.preventDefault();try{const f=new FormData(e.target);const x=await post('/api/setup',{name:f.get('name'),password:f.get('password')});state.token=x.token;state.user=x.user;await render();}catch(err){fail(err);}};}
function renderLogin(){$('#app').innerHTML='<main class="auth"><h1>MusikBot187</h1><p>Anmelden</p><form id="login"><input name="name" placeholder="Benutzername" required><input name="password" type="password" placeholder="Passwort" required><button>Anmelden</button></form></main>';$('#login').onsubmit=async e=>{e.preventDefault();try{const f=new FormData(e.target);const x=await post('/api/login',{name:f.get('name'),password:f.get('password')});state.token=x.token;state.user=x.user;await render();}catch(err){fail(err);}};}
async function setup(){try{const s=await api('/api/setup');s.initialized?renderLogin():renderSetup();}catch(e){$('#app').innerHTML=`<main class="auth"><h1>MusikBot187</h1><p>Backend nicht erreichbar: ${esc(e.message)}</p></main>`;}}
setup();