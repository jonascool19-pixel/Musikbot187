let token=localStorage.mb187||"",view="dashboard",cache=null,edit=false,clockTimer=0;const $=s=>document.querySelector(s);const esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));
async function api(p,o={}){o.headers={...(o.headers||{}),...(token?{Authorization:`Bearer ${token}`}:{})};const r=await fetch(p,o);const t=await r.text();let d={};try{d=t?JSON.parse(t):{}}catch{}if(r.status===401){token="";localStorage.removeItem("mb187");auth();throw Error("Anmeldung erforderlich")};if(!r.ok)throw Error(d.error||`HTTP ${r.status}`);return d} const post=(p,b)=>api(p,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(b||{})});
const titles={dashboard:["Dashboard","Übersicht & Steuerung"],search:["Suche","Musik und Medien finden"],radio:["Radio","Radiosender suchen und abspielen"],playlists:["Playlists","Listen verwalten"],files:["Dateien","Lokale Musik"],instances:["Instanzen","Discord & TeamSpeak 3"],settings:["Einstellungen","System, Netzwerk, Integrationen & Verwaltung"]};
function nav(v){view=v;document.querySelectorAll("#nav button").forEach(x=>x.classList.toggle("active",x.dataset.view===v));render()}
document.querySelectorAll("#nav button").forEach(x=>x.onclick=()=>nav(x.dataset.view));$("#logout").onclick=()=>{token="";localStorage.removeItem("mb187");auth()};$("#theme-control").onclick=async()=>{document.body.classList.toggle("light");await post("/api/settings",{theme:document.body.classList.contains("light")?"light":"dark"});};
function auth(first=false){const m=$("#modal");m.innerHTML=`<div class="modal"><div class="modal-card"><div class="logo big">187</div><h2>${first?"Ersteinrichtung":"Anmeldung"}</h2><p class="muted">Musikbot 187</p><input id="an" placeholder="Benutzername"><input id="ap" type="password" placeholder="Passwort (mind. 10 Zeichen)"><button class="primary wide" id="ago">${first?"Administrator anlegen":"Anmelden"}</button></div></div>`;$("#ago").onclick=async()=>{try{const r=await fetch(first?"/api/setup":"/api/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:$("#an").value,password:$("#ap").value})});const d=await r.json();if(!r.ok)throw Error(d.error);token=d.token;localStorage.mb187=token;m.innerHTML="";render()}catch(e){alert(e.message)}}}
function livebar(s,sys,net){const n=net.interfaces.find(x=>x.name===s.settings.networkInterface)||net.interfaces[0];const cpu=Math.min(100,Math.round((sys.load?.[0]||0)/Math.max(1,sys.cores)*100));const ram=Math.round(sys.ram.used/sys.ram.total*100);$("#livebar").innerHTML=`<span class="chip">CPU ${cpu}%</span><span class="chip">RAM ${ram}%</span><span class="chip">NET ${n?esc(n.name):"—"}</span><span class="chip active-chip">${esc(s.settings.outputType)} · ${esc(s.settings.outputId||"Kein Ausgang")}</span><span class="chip clock" id="clock">${new Date().toLocaleTimeString('de-DE')}</span>`;clearInterval(clockTimer);clockTimer=setInterval(()=>{const c=$("#clock");if(c)c.textContent=new Date().toLocaleTimeString('de-DE')},1000)}
async function render(){if(!token){const setup=await fetch("/api/setup").then(r=>r.json());if(!setup.initialized)auth(true);else auth();return}try{cache=await api("/api/state");const sys=await api("/api/system"),net=await api("/api/network");$("#session").textContent="Angemeldet";$("#title").textContent=titles[view][0];$("#subtitle").textContent=titles[view][1];livebar(cache,sys,net);if(view==="dashboard")dashboard();if(view==="search")searchView("all");if(view==="radio")searchView("radio");if(view==="playlists")playlistView();if(view==="files")fileView();if(view==="instances")instanceView();if(view==="settings")settingsView()}catch(e){console.error(e)}}
function dashboard(){const c=$("#content");const tile=cache.dashboard.map((t,i)=>`<div class="tile ${edit?'editing':''} theme-${esc(t.theme)}" draggable="${edit}" data-id="${t.id}"><div class="tile-top"><span class="icon">${t.icon}</span><span class="lock">${edit?'↕':'🔒'}</span></div><h3>${esc(t.title)}</h3><div class="muted small">${t.id==="now"?esc(cache.current?.title||"Keine Wiedergabe"):t.id==="queue"?`${cache.queue.length} Titel`:t.id==="mode"?cache.mode:t.id==="volume"?`${cache.volume}%`:"Schnellzugriff"}</div></div>`).join("");c.innerHTML=`<div class="setup-banner"><div><b>Musikbot 187</b><span>Systemsteuerung & Wiedergabe</span></div><div class="banner-time">${new Date().toLocaleTimeString('de-DE')}</div><div class="actions"><button onclick="control('restart-bot')">Bot neu starten</button><button onclick="control('restart-system')">System neu starten</button><button class="danger" onclick="control('shutdown-system')">System ausschalten</button></div></div><div class="hero-grid"><div class="card now"><div class="muted">JETZT LÄUFT</div><h2>${esc(cache.current?.title||"Keine Wiedergabe")}</h2><p class="muted">${cache.current?esc(cache.current.source):"Keine aktive Ausgabe"}</p><div class="transport"><button onclick="act('pause')">Ⅱ Pause</button><button onclick="act('resume')">▶ Resume</button><button onclick="act('stop')">■ Stop</button><button onclick="act('skip')">» Skip</button></div></div><div class="card output"><div class="muted">AKTIVE INSTANZ</div><select onchange="setOut(this.value)"><option value="none:">Kein Ausgang</option>${cache.discord.map(x=>`<option value="discord:${x.id}" ${cache.settings.outputType==='discord'&&cache.settings.outputId===x.id?'selected':''}>Discord · ${esc(x.name)}</option>`).join("")}${cache.ts3.map(x=>`<option value="ts3:${x.id}" ${cache.settings.outputType==='ts3'&&cache.settings.outputId===x.id?'selected':''}>TS3 · ${esc(x.name)}</option>`).join("")}</select><label>Lautstärke <input type="range" min="0" max="100" value="${cache.volume}" oninput="vol(this.value)"></label><select onchange="mode(this.value)"><option value="queue" ${cache.mode==='queue'?'selected':''}>Queue</option><option value="repeat" ${cache.mode==='repeat'?'selected':''}>Repeat</option><option value="shuffle" ${cache.mode==='shuffle'?'selected':''}>Shuffle</option></select></div></div><h2 class="section">Dashboard-Baukasten <button class="smallbtn" onclick="edit=!edit;dashboard()">${edit?'Fertig':'Bearbeiten'}</button></h2><div id="tiles" class="tile-grid">${tile}</div><h2 class="section">Queue</h2><div class="card">${cache.queue.length?cache.queue.map((x,i)=>`<div class="rowline"><div><b>${esc(x.title)}</b><span class="muted small">${esc(x.source)}</span></div><button onclick="removeQ(${i})">Entfernen</button></div>`).join(""):"<div class='empty'>Queue ist leer.</div>"}</div><h2 class="section">System & Netzwerk</h2><div class="stats"><div class="card"><b>CPU</b><div class="bar"><i style="width:${Math.min(100,Math.round(sysLoad(cache)))}%"></i></div></div><div class="card"><b>RAM</b><div class="bar"><i style="width:${Math.round((sysLoadRam().used/sysLoadRam().total)*100)}%"></i></div></div><div class="card"><b>Netzwerk</b><p class="muted">${nudge(net)}</p></div></div>`;dragSetup()}
let lastSys=null;async function sysLoadRam(){return(await api('/api/system')).ram}function sysLoad(){return 1}function nudge(n){const i=n.interfaces[0];return i?`↓ ${fmt(i.rx)} · ↑ ${fmt(i.tx)}`:"Keine Karte"}function fmt(n){if(n<1024)return `${Math.round(n)} B`;if(n<1048576)return `${(n/1024).toFixed(1)} KB`;if(n<1073741824)return `${(n/1048576).toFixed(1)} MB`;return `${(n/1073741824).toFixed(1)} GB`}
function dragSetup(){let from="";document.querySelectorAll(".tile[draggable=true]").forEach(x=>{x.ondragstart=()=>from=x.dataset.id;x.ondragover=e=>e.preventDefault();x.ondrop=async()=>{const ids=[...document.querySelectorAll('.tile')].map(t=>t.dataset.id);const a=ids.indexOf(from),b=ids.indexOf(x.dataset.id);if(a<0||b<0)return;ids.splice(a,1);ids.splice(b,0,from);await api('/api/dashboard',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({tiles:ids.map(id=>cache.dashboard.find(t=>t.id===id))})});render()}})}
async function act(a){await post(`/api/play/${a}`);render()}async function vol(v){await post('/api/play/volume',{value:Number(v)})}async function mode(v){await post('/api/play/mode',{mode:v});render()}async function removeQ(i){await api(`/api/queue/${i}`,{method:'DELETE'});render()}async function setOut(v){const [type,id]=v.split(':');await api('/api/settings',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({outputType:type,outputId:id||''})});render()}async function control(a){if(!confirm(a.replaceAll('-',' ')+'?'))return;try{await post('/api/control',{action:a})}catch(e){alert(e.message)}}
function searchView(source){$("#content").innerHTML=`<div class="card search-card"><input id="q" autofocus placeholder="${source==='radio'?'Radiosender suchen…':'Titel, Interpret, URL…'}"><span class="muted small">Die Suche startet automatisch beim Tippen.</span></div><div id="results" class="results"></div>`;let t;$("#q").oninput=()=>{clearTimeout(t);t=setTimeout(()=>doSearch(source),300)}}async function doSearch(source){const q=$("#q").value.trim();if(!q)return;const d=await api(`/api/search?q=${encodeURIComponent(q)}&source=${source}`);$("#results").innerHTML=[...d.youtube,...d.radio,...d.spotify].map(x=>`<div class="result"><img src="${esc(x.thumbnail||'')}" onerror="this.style.display='none'"><div><b>${esc(x.title)}</b><div class="muted small">${esc(x.source)}${x.artist?' · '+esc(x.artist):''}</div></div><div class="actions"><button onclick='playIt(${JSON.stringify(x)})'>▶</button><button onclick='toPlaylist(${JSON.stringify(x)})'>＋</button></div></div>`).join("")||`<div class="empty">Keine Treffer.</div>`}async function playIt(x){await post('/api/play',{items:[x]});render()}async function toPlaylist(x){const ps=await api('/api/playlists');const n=prompt(ps.map(p=>p.name).join('\n')||'Neue Playlist');if(!n)return;let p=ps.find(p=>p.name===n);if(!p)p=await post('/api/playlists',{name:n});await api(`/api/playlists/${p.id}/items`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({items:[x]})});alert('Gespeichert')}
async function playlistView(){const ps=await api('/api/playlists');$("#content").innerHTML=`<div class="rowline"><input id="newpl" placeholder="Neue Playlist"><button onclick="newPl()">Erstellen</button></div><div class="grid">${ps.map(p=>`<div class="card"><div class="rowline"><b>${esc(p.name)}</b><span class="tag">${p.items.length} Titel</span></div><button onclick="playPl('${p.id}')">▶ Starten</button><button onclick="openPl('${p.id}')">Öffnen</button></div>`).join('')||`<div class="empty">Keine Playlists.</div>`}</div>`}async function newPl(){const n=$("#newpl").value.trim();if(n){await post('/api/playlists',{name:n});playlistView()}}async function playPl(id){await post(`/api/playlists/${id}/play`);render()}async function openPl(id){const p=await api(`/api/playlists/${id}`);$("#content").innerHTML=`<div class="card"><div class="rowline"><h2>${esc(p.name)}</h2><button onclick="playPl('${id}')">▶ Starten</button></div>${p.items.map(x=>`<div class="rowline"><span>${esc(x.title)}</span><button onclick="delPl('${id}','${x.id}')">Löschen</button></div>`).join('')||`<div class='empty'>Leer</div>`}</div>`}async function delPl(pid,iid){await api(`/api/playlists/${pid}/items/${iid}`,{method:'DELETE'});openPl(pid)}
async function fileView(){const f=await api('/api/files');$("#content").innerHTML=`<div class="card"><h2>📁 Dateien</h2>${f.map(x=>`<div class="rowline"><span>${esc(x.name)}</span>${x.directory?'':'<button>▶</button>'}</div>`).join('')||`<div class="empty">Keine Dateien.</div>`}</div>`}
async function instanceView(){const [d,t]=await Promise.all([api('/api/discord'),api('/api/ts3')]);$("#content").innerHTML=`<div class="card"><div class="rowline"><div><h2>Discord-Instanzen</h2><span class="muted">Mehrere Bots separat ein-/ausschalten und verwalten.</span></div><button onclick="addDiscord()">＋ Instanz</button></div>${d.map(x=>discordCard(x)).join('')||'<div class="empty">Keine Discord-Instanz.</div>'}</div><div class="card"><div class="rowline"><h2>TeamSpeak 3</h2><button onclick="addTS3()">＋ Instanz</button></div>${t.map(x=>`<div class="instance-mini"><div><b>${esc(x.name)}</b><span class="status ${x.enabled?'on':'off'}">${x.enabled?'AN':'AUS'}</span><div class="muted small">${esc(x.host)}:${x.port} · ${esc(x.channel)}</div></div><div class="actions"><button onclick="toggleTS3('${x.id}',${!x.enabled})">${x.enabled?'Ausschalten':'Einschalten'}</button><button onclick="connTS('${x.id}')">Verbinden</button><button onclick="discTS('${x.id}')">Trennen</button></div></div>`).join('')||'<div class="empty">Keine TS3-Instanz.</div>'}</div>`}
function discordCard(x){const invite=x.clientId?`https://discord.com/oauth2/authorize?client_id=${encodeURIComponent(x.clientId)}&scope=bot%20applications.commands&permissions=36700160`:'';return `<div class="discord-card"><div class="rowline"><div><h3>${esc(x.name)}</h3><span class="status ${x.enabled?'on':'off'}">${x.enabled?'AKTIV':'AUSGESCHALTET'}</span></div><label class="switch"><input type="checkbox" ${x.enabled?'checked':''} onchange="toggleDiscord('${x.id}',this.checked)"><span></span></label></div><div class="form-grid"><label>Name<input id="dn-${x.id}" value="${esc(x.name)}"></label><label>Prefix<input id="dp-${x.id}" value="${esc(x.prefix)}"></label><label>Bot-Token<input id="dt-${x.id}" type="password" placeholder="leer = behalten"></label><label>Client-ID<input id="dc-${x.id}" value="${esc(x.clientId)}"></label><label>Discord-Server<select id="dg-${x.id}"><option value="">Server laden…</option></select></label><label>Voice-Channel<select id="dv-${x.id}" data-sel="${esc(x.channelId)}"><option value="">Server zuerst auswählen…</option></select></label></div><div class="hint">Guild-ID und Voice-Channel-ID werden automatisch aus Discord geladen. Wähle einfach den Server und den gewünschten Voice-Kanal.</div><div class="actions">${invite?`<a class="invite" target="_blank" href="${invite}">➕ Bot zu Discord hinzufügen</a><button onclick="copy('${encodeURIComponent(invite)}')">Link kopieren</button>`:''}<button class="primary" onclick="saveDiscord('${x.id}')">Speichern</button><button onclick="connDC('${x.id}')">Neu verbinden</button><button onclick="joinDC('${x.id}')">Voice beitreten</button><button onclick="discDC('${x.id}')">Trennen</button><button class="danger" onclick="delDC('${x.id}')">Entfernen</button></div></div>`}
async function loadServers(id,sel){try{const a=await api(`/api/discord/${id}/guilds`),s=$(`#dg-${id}`);s.innerHTML='<option value="">Server auswählen…</option>'+a.map(x=>`<option value="${esc(x.id)}" ${x.id===sel?'selected':''}>${esc(x.name)}</option>`).join('');if(sel)loadChannels(id,sel)}catch{}}
async function loadChannels(id,g){const a=await api(`/api/discord/${id}/guilds/${g}/channels`),s=$(`#dv-${id}`),sel=s.dataset.sel||'';s.innerHTML='<option value="">Voice-Kanal auswählen…</option>'+a.map(x=>`<option value="${esc(x.id)}" ${x.id===sel?'selected':''}>${esc(x.name)}</option>`).join('')}
async function saveDiscord(id){const tok=$(`#dt-${id}`).value.trim();await post('/api/discord',{id,name:$(`#dn-${id}`).value,prefix:$(`#dp-${id}`).value,clientId:$(`#dc-${id}`).value,guildId:$(`#dg-${id}`).value,channelId:$(`#dv-${id}`).value,...(tok?{token:tok}:{})});instanceView();}async function toggleDiscord(id,on){await post('/api/discord',{id,enabled:on});if(on){try{await connDC(id)}catch(e){alert(e.message)}}instanceView()}async function addDiscord(){const id=crypto.randomUUID();await post('/api/discord',{id,name:'Discord',enabled:true,prefix:'!',token:'',clientId:'',guildId:'',channelId:''});instanceView()}async function connDC(id){await post(`/api/discord/${id}/connect`);instanceView()}async function discDC(id){await post(`/api/discord/${id}/disconnect`);instanceView()}async function joinDC(id){await post(`/api/discord/${id}/join`);render()}async function delDC(id){if(confirm('Instanz entfernen?')){await api(`/api/discord/${id}`,{method:'DELETE'});instanceView()}}async function copy(s){await navigator.clipboard.writeText(decodeURIComponent(s));alert('Link kopiert')}async function addTS3(){const x={id:crypto.randomUUID(),name:'TS3',enabled:true,host:'',port:9987,channel:'',nickname:'Musikbot 187',password:''};const a=await api('/api/ts3');a.push(x);await api('/api/ts3',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({instances:a})});instanceView()}async function toggleTS3(id,on){const a=await api('/api/ts3');const x=a.find(x=>x.id===id);if(x){x.enabled=on;await api('/api/ts3',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({instances:a})});instanceView()}}async function connTS(id){await post(`/api/ts3/${id}/connect`);instanceView()}async function discTS(id){await post(`/api/ts3/${id}/disconnect`);instanceView()}
async function settingsView(){const [n,d]=await Promise.all([api('/api/network'),api('/api/diagnostics')]);$("#content").innerHTML=`<div class="tabs"><button class="active">🌐 Netzwerk</button><button onclick="integration()">🔗 Integrationen</button><button onclick="users()">👥 Benutzer</button><button onclick="diag()">🩺 Diagnose</button></div><div class="grid"><div class="card"><h3>Netzwerkkarte</h3><select id="net"><option value="auto">Automatisch</option>${n.interfaces.map(x=>`<option value="${esc(x.name)}" ${cache.settings.networkInterface===x.name?'selected':''}>${esc(x.name)}</option>`).join('')}</select><button class="primary" onclick="saveNet()">Speichern</button></div><div class="card"><h3>Gesamtverbrauch</h3>${n.interfaces.map(x=>`<div class="rowline"><span>${esc(x.name)}</span><span>↓ ${fmt(x.rx)} · ↑ ${fmt(x.tx)}</span></div>`).join('')}</div></div>`}async function saveNet(){await api('/api/settings',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({networkInterface:$("#net").value})});render()}async function integration(){const c=$("#content");c.innerHTML=`<div class="card"><h2>Spotify</h2><input id="sid" placeholder="Client-ID"><input id="ss" type="password" placeholder="Client-Secret"><button class="primary" onclick="saveSpotify()">Speichern</button></div>`}async function saveSpotify(){await api('/api/integration/spotify',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({clientId:$("#sid").value,clientSecret:$("#ss").value})});alert('Gespeichert')}async function users(){const u=await api('/api/users');$("#content").innerHTML=`<div class="card"><h2>Benutzer & Rechte</h2>${u.map(x=>`<div class="rowline"><b>${esc(x.name)}</b><span class="tag">${esc(x.role)}</span></div>`).join('')}<button onclick="newUser()">Benutzer anlegen</button></div>`}async function newUser(){const n=prompt('Name');const p=prompt('Passwort');if(n&&p){await post('/api/users',{name:n,password:p,role:'user'});users()}}async function diag(){const d=await api('/api/diagnostics');$("#content").innerHTML=`<div class="card"><h2>Diagnose</h2>${d.map(x=>`<div class="rowline"><span>${esc(x.message)}</span><span class="muted small">${new Date(x.time).toLocaleString('de-DE')}</span></div>`).join('')||'<div class="empty">Keine Fehler.</div>'}</div>`}
(async()=>{const s=await fetch('/api/setup').then(r=>r.json());if(!s.initialized){auth(true);return}if(!token){auth();return}await render();setInterval(()=>view==='dashboard'&&render(),5000);})();
