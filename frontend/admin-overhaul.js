(() => {
  const KEY = 'musikbot187.auth';
  const auth = () => { try { return JSON.parse(sessionStorage.getItem(KEY) || 'null'); } catch { return null; } };
  const esc = value => String(value ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const request = async (path, options = {}) => {
    const a = auth(); const headers = new Headers(options.headers || {});
    if (a?.token) headers.set('Authorization', `Bearer ${a.token}`);
    const response = await fetch(path, {...options, headers});
    const body = await response.json().catch(() => ({}));
    if (!response.ok) throw new Error(body.error || `HTTP ${response.status}`);
    return body;
  };
  const notice = text => { const node = document.querySelector('#notice'); if (!node) return; node.textContent = text; node.classList.add('show'); clearTimeout(window.__adminNoticeTimer); window.__adminNoticeTimer = setTimeout(() => node.classList.remove('show'), 3000); };
  const permissions = { admin:['Player steuern','Playlists verwalten','Verbindungen verwalten','System ansehen','Einstellungen verwalten','Benutzer verwalten','Diagnose ansehen'], user:['Player steuern','Playlists verwalten'] };
  let busy = false;
  async function renderUsers() { const node=document.querySelector('#adminUsersList'); if(!node)return; try { const users=await request('/api/users'); node.innerHTML=users.map(user=>`<div class="admin-user-row"><div><strong>${esc(user.name)}</strong><small>${user.role==='admin'?'Administrator':'Benutzer'}</small></div><div class="admin-rights">${permissions[user.role==='admin'?'admin':'user'].map(p=>`<span>${esc(p)}</span>`).join('')}</div></div>`).join('')||'<p class="muted">Keine Benutzer vorhanden.</p>'; } catch(error){node.innerHTML=`<p class="muted">${esc(error.message)}</p>`;} }
  async function addUser(){ if(busy)return; const name=document.querySelector('#adminNewUser')?.value.trim(); const password=document.querySelector('#adminNewPassword')?.value||''; const role=document.querySelector('#adminNewRole')?.value||'user'; if(!name||!password)return notice('Benutzername und Passwort ausfüllen.'); busy=true; try { await request('/api/users',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,password,role})}); document.querySelector('#adminNewUser').value=''; document.querySelector('#adminNewPassword').value=''; notice('Benutzer wurde angelegt.'); await renderUsers(); } catch(error){notice(error.message);} finally{busy=false;} }
  async function build(){ const view=document.querySelector('#view'); if(!view||document.body.dataset.adminOverhaul==='1')return; document.body.dataset.adminOverhaul='1'; let live={settings:{theme:'dark',accentColor:'#0b69b3',filesDirectory:'music'}}; try { live=await request('/api/state'); } catch {} view.innerHTML=`<section class="admin-section"><div class="sectionhead"><div><h2>👥 Benutzer</h2><small>Konten anlegen und Rollen festlegen</small></div></div><div class="admin-create-grid"><label>Benutzername<input id="adminNewUser" autocomplete="off" maxlength="64" placeholder="z. B. Max"></label><label>Passwort<input id="adminNewPassword" type="password" autocomplete="new-password" minlength="5" maxlength="256" placeholder="Mindestens 5 Zeichen"></label><label>Rolle<select id="adminNewRole"><option value="user">Benutzer</option><option value="admin">Administrator</option></select></label><button id="adminAddUser">＋ Benutzer hinzufügen</button></div><div class="admin-role-help"><b>Rechtesystem</b><span id="adminRolePreview">Benutzer: Player und Playlists</span></div><div id="adminUsersList" class="admin-users-list">Lade Benutzer …</div></section><section class="admin-section"><div class="sectionhead"><div><h2>🎨 Design</h2><small>Darstellung und Akzentfarbe</small></div></div><div class="grid"><label>Theme<select id="themeSelect"><option value="dark">Dunkel</option><option value="light">Hell</option><option value="ocean">Ocean</option></select></label><label>Akzentfarbe<input id="accentColor" type="color" value="${esc(live.settings?.accentColor||'#0b69b3')}"></label><button id="themeSave">Design speichern</button></div></section><section class="admin-section"><div class="sectionhead"><div><h2>🗂 Allgemeine Einstellungen</h2><small>Speicherort für die Musikbibliothek</small></div></div><div class="grid"><label>Dateiverzeichnis<input id="filesDirectory" value="${esc(live.settings?.filesDirectory||'music')}"></label><button id="settingsSave">Einstellungen speichern</button></div></section><section class="admin-section admin-diagnostics"><button id="diagnosticsToggle" class="admin-collapse" aria-expanded="false"><span>🩺 Diagnose</span><span class="admin-chevron">▼</span></button><div id="diagnosticsPanel" hidden><div id="diagnostics" class="diagnostics">Lade Diagnose …</div></div></section>`;
    document.querySelector('#themeSelect').value=live.settings?.theme||'dark';
    document.querySelector('#themeSave').onclick=async()=>{try{const accentColor=document.querySelector('#accentColor').value;await request('/api/settings',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({theme:document.querySelector('#themeSelect').value,accentColor})});document.body.dataset.theme=document.querySelector('#themeSelect').value;notice('Design gespeichert.');}catch(error){notice(error.message);}};
    document.querySelector('#settingsSave').onclick=async()=>{try{await request('/api/settings',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({filesDirectory:document.querySelector('#filesDirectory').value})});notice('Einstellungen gespeichert.');}catch(error){notice(error.message);}};
    document.querySelector('#adminAddUser').onclick=addUser;
    document.querySelector('#adminNewRole').onchange=event=>{document.querySelector('#adminRolePreview').textContent=`${event.target.value==='admin'?'Administrator':'Benutzer'}: ${permissions[event.target.value].join(', ')}`;};
    document.querySelector('#diagnosticsToggle').onclick=async event=>{const button=event.currentTarget;const panel=document.querySelector('#diagnosticsPanel');const open=!panel.hidden;panel.hidden=open;button.setAttribute('aria-expanded',String(!open));button.querySelector('.admin-chevron').textContent=open?'▼':'▲';if(!open&&!panel.dataset.loaded){try{const items=await request('/api/diagnostics');document.querySelector('#diagnostics').innerHTML=items.map(item=>`<div><span>${esc(item.time)} · ${esc(item.level||'')} · ${esc(item.source||'')}</span><small>${esc(item.message)}</small></div>`).join('')||'<p class="muted">Keine Diagnosemeldungen.</p>';panel.dataset.loaded='1';}catch(error){document.querySelector('#diagnostics').textContent=error.message;}}};
    void renderUsers();
  }
  const activate=()=>{if(!document.querySelector('[data-tab="admin"]')||!document.querySelector('#view'))return;if(document.querySelector('#themeSelect')&&document.body.dataset.adminOverhaul==='1')return;setTimeout(build,0);};
  document.addEventListener('click',event=>{if(event.target?.closest?.('[data-tab="admin"]')){document.body.dataset.adminOverhaul='0';activate();}},true);
  new MutationObserver(activate).observe(document.body,{childList:true,subtree:true});
})();
