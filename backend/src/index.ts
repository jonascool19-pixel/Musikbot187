import Fastify from 'fastify';
import cors from '@fastify/cors';
import fastifyStatic from '@fastify/static';
import { Client, GatewayIntentBits, REST, Routes, SlashCommandBuilder, ChannelType } from 'discord.js';
import { AudioPlayerStatus, NoSubscriberBehavior, VoiceConnection, VoiceConnectionStatus, createAudioPlayer, createAudioResource, joinVoiceChannel, StreamType } from '@discordjs/voice';
import { spawn } from 'node:child_process';
import fs from 'node:fs';
import path from 'node:path';
import crypto from 'node:crypto';

const PORT = Number(process.env.PORT ?? 3000);
const APP_ROOT = path.resolve(process.env.APP_ROOT ?? path.join(import.meta.dirname, '../..'));
const DATA_DIR = path.resolve(process.env.DATA_DIR ?? '/var/lib/radiobot');
const MUSIC_DIR = path.join(DATA_DIR, 'music');
const DB_FILE = path.join(DATA_DIR, 'radiobot.json');
const SPOTIFY_FILE = path.join(DATA_DIR, 'spotify.json');
const WEB_USER = process.env.WEB_USER ?? 'admin';
const WEB_PASSWORD = process.env.WEB_PASSWORD ?? '';
const SPOTIFY_CLIENT_ID = process.env.SPOTIFY_CLIENT_ID ?? '';
const SPOTIFY_CLIENT_SECRET = process.env.SPOTIFY_CLIENT_SECRET ?? '';
const SPOTIFY_REDIRECT_URI = process.env.SPOTIFY_REDIRECT_URI ?? '';
fs.mkdirSync(MUSIC_DIR, { recursive: true });
fs.mkdirSync(DATA_DIR, { recursive: true });
interface Radio { id:string; name:string; url:string; enabled:boolean }
interface GuildState { guildId:string; voiceChannelId:string; radioId?:string; playing?:string; playingType?:'radio'|'file'; volume:number; paused:boolean; queue:string[] }
interface Db { radios:Radio[]; guilds:Record<string,GuildState> }
interface SpotifyState { accessToken?:string; refreshToken?:string; expiresAt?:number; displayName?:string; product?:string }
function loadJson<T>(file:string,fallback:T):T{try{return JSON.parse(fs.readFileSync(file,'utf8')) as T}catch{return fallback}}
function saveJson(file:string,value:unknown){fs.writeFileSync(file,JSON.stringify(value,null,2),{mode:0o600})}
function id(){return crypto.randomBytes(5).toString('hex')}
const db=loadJson<Db>(DB_FILE,{radios:[],guilds:{}});const spotify=loadJson<SpotifyState>(SPOTIFY_FILE,{});let spotifyOAuthState:string|undefined;
const connections=new Map<string,VoiceConnection>(),players=new Map<string,ReturnType<typeof createAudioPlayer>>(),currentProcesses=new Map<string,ReturnType<typeof spawn>>();
function getState(guildId:string):GuildState{db.guilds[guildId]??={guildId,voiceChannelId:'',volume:80,paused:false,queue:[]};return db.guilds[guildId]}
function checkAuth(req:any,reply:any){if(!WEB_PASSWORD)return true;const h=String(req.headers.authorization??'');if(!h.startsWith('Basic ')){reply.header('WWW-Authenticate','Basic realm="RadioBot"').code(401).send('Authentication required');return false}const d=Buffer.from(h.slice(6),'base64').toString('utf8'),i=d.indexOf(':');const u=i>=0?d.slice(0,i):'',p=i>=0?d.slice(i+1):'';if(u!==WEB_USER||p!==WEB_PASSWORD){reply.header('WWW-Authenticate','Basic realm="RadioBot"').code(401).send('Invalid credentials');return false}return true}
function makePlayer(guildId:string){let p=players.get(guildId);if(!p){p=createAudioPlayer({behaviors:{noSubscriber:NoSubscriberBehavior.Stop}});p.on(AudioPlayerStatus.Idle,()=>{currentProcesses.get(guildId)?.kill('SIGTERM');currentProcesses.delete(guildId);playNext(guildId).catch(console.error)});p.on('error',e=>console.error('audio error',guildId,e));players.set(guildId,p)}return p}
function sourceToPath(source:string){const n=source.slice(5),r=path.resolve(MUSIC_DIR,n);if(!r.startsWith(MUSIC_DIR+path.sep))throw new Error('Ungültige Mediendatei');return r}
async function ensureConnection(guildId:string){const s=getState(guildId);if(!s.voiceChannelId)throw new Error('Kein Voice-Channel konfiguriert.');const g=await client.guilds.fetch(guildId),c=await g.channels.fetch(s.voiceChannelId);if(!c||c.type!==ChannelType.GuildVoice)throw new Error('Voice-Channel nicht gefunden.');const old=connections.get(guildId);if(old&&old.state.status!==VoiceConnectionStatus.Destroyed)return old;const conn=joinVoiceChannel({channelId:c.id,guildId:g.id,adapterCreator:g.voiceAdapterCreator});conn.on('error',e=>console.error('voice error',guildId,e));connections.set(guildId,conn);conn.subscribe(makePlayer(guildId));return conn}
async function playNext(guildId:string){const s=getState(guildId),src=s.queue.shift();if(!src){s.playing=undefined;s.playingType=undefined;saveJson(DB_FILE,db);return}s.playing=src.startsWith('file:')?src.slice(5):src;s.playingType=src.startsWith('file:')?'file':'radio';s.paused=false;saveJson(DB_FILE,db);const conn=await ensureConnection(guildId),p=makePlayer(guildId),input=src.startsWith('file:')?sourceToPath(src):src,args=src.startsWith('file:')?['-hide_banner','-loglevel','error','-i',input,'-vn','-f','s16le','-ar','48000','-ac','2','pipe:1']:['-hide_banner','-loglevel','error','-reconnect','1','-reconnect_streamed','1','-reconnect_delay_max','5','-i',input,'-vn','-f','s16le','-ar','48000','-ac','2','pipe:1'];const ff=spawn('ffmpeg',args,{stdio:['ignore','pipe','inherit']});currentProcesses.set(guildId,ff);ff.on('exit',()=>{if(currentProcesses.get(guildId)===ff)currentProcesses.delete(guildId)});const resource=createAudioResource(ff.stdout,{inputType:StreamType.Raw,inlineVolume:true});resource.volume?.setVolume(Math.max(0,Math.min(2,s.volume/100)));conn.subscribe(p);p.play(resource)}
async function stopGuild(guildId:string){const s=getState(guildId);s.queue=[];s.playing=undefined;s.playingType=undefined;players.get(guildId)?.stop(true);currentProcesses.get(guildId)?.kill('SIGTERM');currentProcesses.delete(guildId);connections.get(guildId)?.destroy();connections.delete(guildId);saveJson(DB_FILE,db)}
async function spotifyToken():Promise<string>{if(spotify.accessToken&&spotify.expiresAt&&spotify.expiresAt>Date.now()+30000)return spotify.accessToken;if(!SPOTIFY_CLIENT_ID||!SPOTIFY_CLIENT_SECRET||!spotify.refreshToken)throw new Error('Spotify ist noch nicht verbunden.');const basic=Buffer.from(`${SPOTIFY_CLIENT_ID}:${SPOTIFY_CLIENT_SECRET}`).toString('base64'),r=await fetch('https://accounts.spotify.com/api/token',{method:'POST',headers:{Authorization:`Basic ${basic}`,'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams({grant_type:'refresh_token',refresh_token:spotify.refreshToken})});if(!r.ok)throw new Error(`Spotify token refresh fehlgeschlagen (${r.status})`);const d=await r.json() as any;spotify.accessToken=d.access_token;spotify.expiresAt=Date.now()+Number(d.expires_in??3600)*1000;if(d.refresh_token)spotify.refreshToken=d.refresh_token;saveJson(SPOTIFY_FILE,spotify);return spotify.accessToken}
async function spotifyApi(endpoint:string,init:RequestInit={},retry=true):Promise<any>{const token=await spotifyToken(),headers=new Headers(init.headers);headers.set('Authorization',`Bearer ${token}`);if(init.body&&!headers.has('Content-Type'))headers.set('Content-Type','application/json');const r=await fetch(`https://api.spotify.com/v1${endpoint}`,{...init,headers});if(r.status===401&&retry&&spotify.refreshToken){spotify.expiresAt=0;await spotifyToken();return spotifyApi(endpoint,init,false)}if(!r.ok)throw new Error(`Spotify API ${r.status}: ${await r.text()}`);if(r.status===204)return null;return r.json()}
function spotifyPlaylistId(input:string){return input.match(/(?:open\.spotify\.com\/playlist\/|spotify:playlist:)([A-Za-z0-9]+)/)?.[1]??null}
const client=new Client({intents:[GatewayIntentBits.Guilds,GatewayIntentBits.GuildVoiceStates]});const commands=[new SlashCommandBuilder().setName('radio').setDescription('Radio starten').addStringOption(o=>o.setName('name').setDescription('Sendername').setRequired(true)),new SlashCommandBuilder().setName('stop').setDescription('Wiedergabe stoppen'),new SlashCommandBuilder().setName('skip').setDescription('Nächste Quelle abspielen'),new SlashCommandBuilder().setName('volume').setDescription('Lautstärke setzen').addIntegerOption(o=>o.setName('percent').setDescription('0-100').setRequired(true).setMinValue(0).setMaxValue(100)),new SlashCommandBuilder().setName('join').setDescription('Bot in deinen Voice-Channel holen')].map(c=>c.toJSON());
client.once('ready',async()=>{console.log(`Discord online als ${client.user?.tag}`);try{await new REST({version:'10'}).setToken(process.env.DISCORD_TOKEN!).put(Routes.applicationCommands(client.user!.id),{body:commands})}catch(e){console.error(e)}});
client.on('interactionCreate',async i=>{if(!i.isChatInputCommand()||!i.guildId)return;const s=getState(i.guildId);try{if(i.commandName==='join'){const m=await i.guild!.members.fetch(i.user.id);if(!m.voice.channel||m.voice.channel.type!==ChannelType.GuildVoice)return void i.reply('Du musst selbst in einem Voice-Channel sein.');s.voiceChannelId=m.voice.channel.id;saveJson(DB_FILE,db);await ensureConnection(i.guildId);return void i.reply(`🔊 Verbunden mit **${m.voice.channel.name}**`)}if(i.commandName==='stop'){await stopGuild(i.guildId);return void i.reply('⏹️ Gestoppt.')}if(i.commandName==='skip'){players.get(i.guildId)?.stop(true);return void i.reply('⏭️ Übersprungen.')}if(i.commandName==='volume'){const v=i.options.getInteger('percent',true);s.volume=v;saveJson(DB_FILE,db);return void i.reply(`🔊 Lautstärke: ${v}%`)}if(i.commandName==='radio'){const n=i.options.getString('name',true),r=db.radios.find(x=>x.name.toLowerCase()===n.toLowerCase());if(!r)return void i.reply('Sender nicht gefunden.');s.queue=[r.url];s.radioId=r.id;saveJson(DB_FILE,db);await playNext(i.guildId);return void i.reply(`📻 ${r.name}`)}}catch(e){console.error(e);if(!i.replied)await i.reply(`Fehler: ${e instanceof Error?e.message:'Unbekannt'}`)}});
const app=Fastify({logger:true});await app.register(cors,{origin:true});await app.register(fastifyStatic,{root:path.join(APP_ROOT,'frontend'),prefix:'/',index:['index.html']});app.addHook('preHandler',async(req,reply)=>{if(req.url.startsWith('/api/')&&!req.url.startsWith('/api/spotify/callback')&&!checkAuth(req,reply))return reply});
app.get('/api/health',async()=>({ok:true,discord:client.isReady(),version:'1.0.0'}));app.get('/api/guilds',async()=>client.guilds.cache.map(g=>({id:g.id,name:g.name})));app.get<{Params:{id:string}}>('/api/guilds/:id/channels',async req=>{const g=await client.guilds.fetch(req.params.id);return g.channels.cache.filter(c=>c.type===ChannelType.GuildVoice).map(c=>({id:c.id,name:c.name}))});app.get('/api/radios',async()=>db.radios);app.post<{Body:{name:string;url:string}}>('/api/radios',async req=>{const r:Radio={id:id(),name:req.body.name.trim(),url:req.body.url.trim(),enabled:true};db.radios.push(r);saveJson(DB_FILE,db);return r});app.delete<{Params:{id:string}}>('/api/radios/:id',async req=>{db.radios=db.radios.filter(r=>r.id!==req.params.id);saveJson(DB_FILE,db);return{ok:true}});app.get('/api/media',async()=>fs.readdirSync(MUSIC_DIR).filter(f=>/\.(mp3|wav|ogg|flac|m4a)$/i.test(f)).sort());app.get<{Params:{id:string}}>('/api/state/:id',async req=>getState(req.params.id));app.post<{Params:{id:string};Body:{voiceChannelId:string}}>('/api/state/:id/voice',async req=>{const s=getState(req.params.id);s.voiceChannelId=req.body.voiceChannelId;saveJson(DB_FILE,db);return s});app.post<{Params:{id:string};Body:{radioId:string}}>('/api/state/:id/radio',async req=>{const s=getState(req.params.id),r=db.radios.find(x=>x.id===req.body.radioId);if(!r)throw new Error('Radio nicht gefunden');s.queue=[r.url];s.radioId=r.id;saveJson(DB_FILE,db);await playNext(req.params.id);return s});app.post<{Params:{id:string};Body:{file:string}}>('/api/state/:id/file',async req=>{const s=getState(req.params.id),file=sourceToPath(`file:${req.body.file}`);if(!fs.existsSync(file))throw new Error('Datei nicht gefunden');s.queue=[`file:${req.body.file}`];saveJson(DB_FILE,db);await playNext(req.params.id);return s});app.post<{Params:{id:string}}>('/api/state/:id/stop',async req=>{await stopGuild(req.params.id);return getState(req.params.id)});app.post<{Params:{id:string}}>('/api/state/:id/skip',async req=>{players.get(req.params.id)?.stop(true);return getState(req.params.id)});app.post<{Params:{id:string};Body:{volume:number}}>('/api/state/:id/volume',async req=>{const s=getState(req.params.id);s.volume=Math.max(0,Math.min(100,Number(req.body.volume)));saveJson(DB_FILE,db);return s});
app.get('/api/spotify/status',async()=>({configured:Boolean(SPOTIFY_CLIENT_ID&&SPOTIFY_CLIENT_SECRET&&SPOTIFY_REDIRECT_URI),connected:Boolean(spotify.refreshToken),displayName:spotify.displayName??null,product:spotify.product??null}));app.get('/api/spotify/login',async(_req,reply)=>{if(!SPOTIFY_CLIENT_ID||!SPOTIFY_CLIENT_SECRET||!SPOTIFY_REDIRECT_URI)return reply.code(400).send('Spotify-Konfiguration fehlt');spotifyOAuthState=crypto.randomBytes(18).toString('hex');const q=new URLSearchParams({response_type:'code',client_id:SPOTIFY_CLIENT_ID,redirect_uri:SPOTIFY_REDIRECT_URI,scope:'user-read-playback-state user-modify-playback-state playlist-read-private playlist-read-collaborative',state:spotifyOAuthState});return reply.redirect(`https://accounts.spotify.com/authorize?${q}`)});app.get<{Querystring:{code?:string;state?:string}}>('/api/spotify/callback',async(req,reply)=>{if(!req.query.code||!req.query.state||req.query.state!==spotifyOAuthState)return reply.code(400).send('Ungültiger Spotify-OAuth-State.');spotifyOAuthState=undefined;const basic=Buffer.from(`${SPOTIFY_CLIENT_ID}:${SPOTIFY_CLIENT_SECRET}`).toString('base64'),r=await fetch('https://accounts.spotify.com/api/token',{method:'POST',headers:{Authorization:`Basic ${basic}`,'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams({grant_type:'authorization_code',code:req.query.code,redirect_uri:SPOTIFY_REDIRECT_URI})});if(!r.ok)return reply.code(400).send('Spotify-Token konnte nicht erstellt werden.');const t=await r.json() as any;spotify.accessToken=t.access_token;spotify.refreshToken=t.refresh_token;spotify.expiresAt=Date.now()+Number(t.expires_in??3600)*1000;const me=await spotifyApi('/me');spotify.displayName=me.display_name??me.id;spotify.product=me.product;saveJson(SPOTIFY_FILE,spotify);return reply.redirect('/?spotify=connected')});app.get<{Querystring:{q:string}}>('/api/spotify/search',async req=>{const q=String(req.query.q??'').trim();if(!q)return{tracks:[]};const d=await spotifyApi(`/search?${new URLSearchParams({q,type:'track',limit:'20'})}`);return{tracks:d.tracks.items.map((t:any)=>({id:t.id,uri:t.uri,name:t.name,artist:t.artists.map((a:any)=>a.name).join(', '),album:t.album.name,url:t.external_urls.spotify}))}});app.get('/api/spotify/devices',async()=>{const d=await spotifyApi('/me/player/devices');return d.devices.map((x:any)=>({id:x.id,name:x.name,type:x.type,active:x.is_active,volume:x.volume_percent}))});app.post<{Body:{uri:string;deviceId?:string}}>('/api/spotify/play',async req=>{await spotifyApi('/me/player/play'+(req.body.deviceId?`?device_id=${encodeURIComponent(req.body.deviceId)}`:''),{method:'PUT',body:JSON.stringify({uris:[req.body.uri]})});return{ok:true}});app.post('/api/spotify/pause',async()=>{await spotifyApi('/me/player/pause',{method:'PUT'});return{ok:true}});app.post('/api/spotify/next',async()=>{await spotifyApi('/me/player/next',{method:'POST'});return{ok:true}});app.get('/api/spotify/current',async()=>{try{const d=await spotifyApi('/me/player');return d?{playing:d.is_playing,track:d.item?{name:d.item.name,artist:d.item.artists?.map((a:any)=>a.name).join(', '),url:d.item.external_urls?.spotify}:null,device:d.device?.name??null}:{playing:false,track:null,device:null}}catch{return{playing:false,track:null,device:null}}});app.post<{Body:{url:string}}>('/api/spotify/playlist-info',async req=>{const id=spotifyPlaylistId(req.body.url);if(!id)throw new Error('Keine Spotify-Playlist-URL');const d=await spotifyApi(`/playlists/${id}`);return{id:d.id,name:d.name,url:d.external_urls?.spotify,owner:d.owner?.display_name,tracks:d.tracks?.total??0}});
await app.listen({port:PORT,host:'0.0.0.0'});if(process.env.DISCORD_TOKEN)client.login(process.env.DISCORD_TOKEN).catch(console.error);else console.warn('DISCORD_TOKEN fehlt. Weboberfläche läuft, Discord ist offline.');
