import Fastify from "fastify";import cors from "@fastify/cors";import statik from "@fastify/static";import {mkdir,readFile,writeFile} from "node:fs/promises";import path from "node:path";import os from "node:os";import {spawn} from "node:child_process";import {PassThrough} from "node:stream";import {randomUUID,scryptSync,timingSafeEqual} from "node:crypto";import {Client,GatewayIntentBits,ChannelType} from "discord.js";import {joinVoiceChannel,createAudioPlayer,createAudioResource,StreamType} from "@discordjs/voice";import {Client as TSClient,generateIdentity} from "@honeybbq/teamspeak-client";import OpusScript from "opusscript";
const ROOT=process.env.RADIOBOT_DATA||"/var/lib/radiobot",PORT=Number(process.env.PORT||3000),HOST=process.env.HOST||"0.0.0.0",FRONTEND=path.resolve(process.env.RADIOBOT_FRONTEND||path.join(process.cwd(),"../frontend")),YTDLP=process.env.YTDLP_PATH||"yt-dlp",FFMPEG=process.env.FFMPEG_PATH||"ffmpeg",DATA=path.join(ROOT,"state.json");
type Item={id:string,title:string,url:string,source:string,duration?:number,thumbnail?:string};type User={id:string,name:string,hash:string,role:"admin"|"user"};type State={users:User[];playlists:{id:string,name:string,items:Item[]}[];settings:any;dashboard:{id:string,title:string,color:string,icon:string,locked:boolean}[];discord:any[];ts3:any[]};
const defaults:State={users:[],playlists:[],settings:{volume:80,mode:"queue",activeInstance:"default",networkInterface:"auto",discordBotClientId:"",prefix":"!"},dashboard:[{id:"now",title:"Jetzt läuft",color:"violet",icon:"🎵",locked:true},{id:"queue",title:"Queue",color:"blue",icon:"📋",locked:true},{id:"search",title:"Suche",color:"green",icon:"🔎",locked:true},{id:"radio",title:"Radio",color:"orange",icon:"📻",locked:true},{id:"system",title:"System",color:"pink",icon:"🖥️",locked:true},{id:"network",title:"Netzwerk",color:"cyan",icon:"🌐",locked:true},{id:"instances",title:"Instanzen",color:"indigo",icon:"🎧",locked:true},{id:"playlists",title:"Playlists",color:"amber",icon:"💿",locked:true}],discord:[],ts3:[]};let state:State=structuredClone(defaults);let saveTimer:any;async function save(){clearTimeout(saveTimer);saveTimer=setTimeout(()=>writeFile(DATA,JSON.stringify(state,null,2)),50)}async function load(){await mkdir(ROOT,{recursive:true});try{state=JSON.parse(await readFile(DATA,"utf8"))}catch{await save()}}
function hash(p:string){return scryptSync(p,"radiobot-v4",32).toString("hex")}function good(u:User,p:string){const a=Buffer.from(u.hash,"hex"),b=Buffer.from(hash(p),"hex");return a.length===b.length&&timingSafeEqual(a,b)}const sessions=new Map<string,string>();function auth(req:any){const t=String(req.headers.authorization||"").replace(/^Bearer /,"");return state.users.find(u=>u.id===sessions.get(t))}
async function js(url:string,init?:RequestInit){const r=await fetch(url,init);if(!r.ok)throw new Error(`${r.status} ${r.statusText}`);return r.json()}
function ytSearch(q:string):Promise<Item[]>{return new Promise((res,rej)=>{const p=spawn(YTDLP,[`ytsearch8:${q}`,"--flat-playlist","--dump-single-json","--no-warnings"]);let s="";p.stdout.on("data",d=>s+=d);p.on("error",rej);p.on("close",c=>{if(c)return rej(new Error("yt-dlp search failed"));try{const j=JSON.parse(s);res((j.entries||[]).map((x:any)=>({id:x.id||randomUUID(),title:x.title,url:x.url||`https://www.youtube.com/watch?v=${x.id}`,duration:x.duration,source:"youtube",thumbnail:x.thumbnails?.[0]?.url})))}catch(e){rej(e)}})})}async function radioSearch(q:string):Promise<Item[]>{const j=await js("https://de1.api.radio-browser.info/json/stations/search?limit=15&hidebroken=true&order=clickcount&reverse=true&name="+encodeURIComponent(q));return j.map((x:any)=>({id:x.stationuuid,title:x.name,url:x.url_resolved||x.url,source:"radio",thumbnail:x.favicon}))}let spToken="",spExp=0;async function spotifySearch(q:string):Promise<Item[]>{const id=process.env.SPOTIFY_CLIENT_ID,secret=process.env.SPOTIFY_CLIENT_SECRET;if(!id||!secret)return[];if(Date.now()>spExp){const b=Buffer.from(`${id}:${secret}`).toString("base64"),r=await js("https://accounts.spotify.com/api/token",{method:"POST",headers:{Authorization:`Basic ${b}`,"Content-Type":"application/x-www-form-urlencoded"},body:"grant_type=client_credentials"});spToken=r.access_token;spExp=Date.now()+((r.expires_in||3600)-60)*1000}const j=await js("https://api.spotify.com/v1/search?type=track&limit=8&q="+encodeURIComponent(q),{headers:{Authorization:`Bearer ${spToken}`}});return (j.tracks?.items||[]).map((x:any)=>({id:x.id,title:`${x.name} — ${x.artists?.map((a:any)=>a.name).join(", ")}`,url:x.external_urls?.spotify||"",source:"spotify",duration:x.duration_ms/1000,thumbnail:x.album?.images?.[0]?.url}))}
async function streamUrl(x:Item){if(x.source==="radio")return x.url;if(x.source==="spotify")return (await ytSearch(x.title))[0]?.url;return new Promise<string>((res,rej)=>{const p=spawn(YTDLP,[x.url,"-g","-f","bestaudio/best","--no-playlist","--no-warnings"]);let s="";p.stdout.on("data",d=>s+=d);p.on("close",c=>c?rej(new Error("stream resolve failed")):res(s.trim().split(/\r?\n/)[0]||x.url));p.on("error",rej)})}
class Player{q:Item[]=[];current:Item|null=null;paused=false;volume=80;mode="queue";ff:ReturnType<typeof spawn>|undefined;listeners=new Set<(b:Buffer)=>void>();async enqueue(xs:Item[]){this.q.push(...xs);if(!this.current)await this.next()}async next(){this.stopProc();this.current=this.q.shift()||null;this.paused=false;if(this.current){try{const u=await streamUrl(this.current);this.ff=spawn(FFMPEG,["-hide_banner","-loglevel","error","-reconnect","1","-reconnect_streamed","1","-reconnect_delay_max","5","-i",u,"-vn","-f","s16le","-ar","48000","-ac","2","pipe:1"]);this.ff.stdout?.on("data",d=>this.listeners.forEach(fn=>fn(Buffer.from(d))));this.ff.on("close",()=>{if(this.current&&!this.paused)void this.next()})}catch{this.current=null;return this.next()}}}pause(){if(this.ff){this.ff.kill("SIGSTOP");this.paused=true}}resume(){if(this.ff){this.ff.kill("SIGCONT");this.paused=false}}stop(){this.stopProc();this.current=null;this.paused=false}skip(){this.stopProc();void this.next()}setVolume(v:number){this.volume=Math.max(0,Math.min(100,v))}stopProc(){if(this.ff){this.ff.kill("SIGTERM");this.ff=undefined}}snapshot(){return{current:this.current,queue:this.q,paused:this.paused,volume:this.volume,mode:this.mode}}}
class DiscordOut{instances=new Map<string,any>();async connect(c:any){const client=new Client({intents:[GatewayIntentBits.Guilds,GatewayIntentBits.GuildVoiceStates]});await client.login(c.token);this.instances.set(c.id,{...c,client,voice:null,player:null,stream:null});return this.status()}async disconnect(id:string){const x=this.instances.get(id);if(x){x.voice?.destroy();await x.client.destroy();this.instances.delete(id)}}async join(id:string){const x=this.instances.get(id);if(!x)throw new Error("Discord-Instanz nicht verbunden");const g=x.client.guilds.cache.get(x.guildId);if(!g)throw new Error("Guild nicht gefunden");x.voice=joinVoiceChannel({guildId:g.id,channelId:x.channelId,adapterCreator:g.voiceAdapterCreator});x.player=createAudioPlayer();x.voice.subscribe(x.player);x.stream=new PassThrough();x.player.play(createAudioResource(x.stream,{inputType:StreamType.Raw}));}audio(b:Buffer){for(const x of this.instances.values())x.stream?.write(b)}status(){return [...this.instances.values()].map(x=>({id:x.id,name:x.name,connected:true,guildId:x.guildId,channelId:x.channelId,guilds:x.client.guilds.cache.size,prefix:x.prefix}))}guilds(id:string){const x=this.instances.get(id);return x?[...x.client.guilds.cache.values()].map((g:any)=>({id:g.id,name:g.name})):[]}channels(id:string,gid:string){const x=this.instances.get(id),g=x?.client.guilds.cache.get(gid);return g?[...g.channels.cache.values()].filter((c:any)=>c.type===ChannelType.GuildVoice).map((c:any)=>({id:c.id,name:c.name})):[]}}
class TSOut{configs:any[]=[];clients=new Map<string,any>();encoder=new OpusScript(48000,2,OpusScript.Application.AUDIO);pcm=Buffer.alloc(0);set(c:any[]){this.configs=c}async connect(id:string){const c=this.configs.find(x=>x.id===id);if(!c)throw new Error("TS3-Instanz nicht gefunden");const cl=new TSClient(generateIdentity(8),c.host,c.nickname,{serverPassword:c.password,defaultChannel:c.channel});await cl.connect();await cl.waitConnected(AbortSignal.timeout(15000));this.clients.set(id,cl);return this.status()}async disconnect(id:string){const c=this.clients.get(id);if(c){await c.disconnect();this.clients.delete(id)}}audio(b:Buffer){this.pcm=Buffer.concat([this.pcm,b]);while(this.pcm.length>=3840){const f=this.pcm.subarray(0,3840);this.pcm=this.pcm.subarray(3840);const o=this.encoder.encode(f,960);for(const c of this.clients.values())c.sendVoice(o,4)}}status(){return this.configs.map(c=>({...c,password:undefined,connected:this.clients.has(c.id)}))}}
function sys(){const m=os.totalmem(),free=os.freemem();return{hostname:os.hostname(),platform:process.platform,node:process.version,cores:os.cpus().length,load:os.loadavg(),ram:{total:m,used:m-free,free},uptime:os.uptime()}}async function net(){const out=[];const ifs=os.networkInterfaces();for(const [name,vals] of Object.entries(ifs)){const rx=await readFile(`/sys/class/net/${name}/statistics/rx_bytes`,"utf8").catch(()=>"0"),tx=await readFile(`/sys/class/net/${name}/statistics/tx_bytes`,"utf8").catch(()=>"0");out.push({name,rx:Number(rx),tx:Number(tx),addresses:(vals||[]).map(v=>v.address)})}return{interfaces:out,selected:state.settings.networkInterface}}
const player=new Player(),discord=new DiscordOut(),ts3=new TSOut(),app=Fastify({logger:true});player.listeners.add(b=>discord.audio(b));player.listeners.add(b=>ts3.audio(b));await load();await app.register(cors,{origin:true});await app.register(statik,{root:FRONTEND,prefix:"/"});
app.get("/api/setup",()=>({initialized:state.users.length>0}));app.post("/api/setup",async(req:any,rep)=>{if(state.users.length)return rep.code(409).send({error:"already initialized"});const b=req.body||{};if(!b.name||String(b.password||"").length<10)return rep.code(400).send({error:"Benutzername und Passwort (min. 10 Zeichen) erforderlich"});state.users.push({id:randomUUID(),name:b.name,hash:hash(b.password),role:"admin"});await save();const t=randomUUID();sessions.set(t,state.users[0].id);return{token:t}});app.post("/api/login",(req:any,rep)=>{const b=req.body||{},u=state.users.find(x=>x.name===b.name);if(!u||!good(u,b.password||""))return rep.code(401).send({error:"Ungültige Anmeldung"});const t=randomUUID();sessions.set(t,u.id);return{token:t,user:{id:u.id,name:u.name,role:u.role}}});app.addHook("preHandler",async(req:any,rep)=>{if(req.url.startsWith("/api/setup")||req.url.startsWith("/api/login")||req.url==="/api/health")return;if(req.url.startsWith("/api/")&&!auth(req))return rep.code(401).send({error:"unauthorized"})});app.get("/api/health",()=>({ok:true,version:"4.0.0"}));
app.get("/api/state",()=>({...player.snapshot(),dashboard:state.dashboard,settings:state.settings,discord:discord.status(),ts3:ts3.status()}));app.post("/api/play",async(req:any)=>{await player.enqueue((req.body?.items||[]).map((x:any)=>({...x,id:x.id||randomUUID()})));return player.snapshot()});app.post("/api/play/:action",async(req:any)=>{const p=req.params as {action:string};if(p.action==="pause")player.pause();if(p.action==="resume")player.resume();if(p.action==="stop")player.stop();if(p.action==="skip")player.skip();if(p.action==="volume")player.setVolume(Number(req.body?.value));return player.snapshot()});
app.get("/api/search",async(req:any)=>{const q=String((req.query as any)?.q||"").trim();if(!q)return{youtube:[],radio:[],spotify:[]};const source=String((req.query as any)?.source||"all");const out:any={youtube:[],radio:[],spotify:[]};await Promise.allSettled([source==="all"||source==="youtube"?ytSearch(q).then(x=>out.youtube=x):Promise.resolve(),source==="all"||source==="radio"?radioSearch(q).then(x=>out.radio=x):Promise.resolve(),source==="all"||source==="spotify"?spotifySearch(q).then(x=>out.spotify=x):Promise.resolve()]);return out});
app.get("/api/playlists",()=>state.playlists);app.post("/api/playlists",async(req:any)=>{const p={id:randomUUID(),name:String(req.body?.name||"Neue Playlist"),items:[] as Item[]};state.playlists.push(p);await save();return p});app.post("/api/playlists/:id/items",async(req:any)=>{const p=state.playlists.find(x=>x.id===(req.params as {id:string}).id);if(!p)return{error:"not found"};p.items.push(...(req.body?.items||[]));await save();return p});app.post("/api/playlists/:id/play",async(req:any)=>{const p=state.playlists.find(x=>x.id===(req.params as {id:string}).id);if(p)await player.enqueue(p.items);return player.snapshot()});app.delete("/api/playlists/:id/items/:itemId",async(req:any)=>{const p=req.params as {id:string,itemId:string},pl=state.playlists.find(x=>x.id===p.id);if(pl)pl.items=pl.items.filter(x=>x.id!==p.itemId);await save();return pl});
app.get("/api/dashboard",()=>({tiles:state.dashboard}));app.put("/api/dashboard",async(req:any)=>{const ids=(req.body?.tiles||[]).filter((x:string)=>state.dashboard.some(t=>t.id===x));state.dashboard=ids.map((id:string)=>state.dashboard.find(t=>t.id===id)!);await save();return{tiles:state.dashboard}});app.put("/api/settings",async(req:any)=>{state.settings={...state.settings,...req.body};await save();return state.settings});app.get("/api/system",sys);app.get("/api/network",net);
app.get("/api/discord/status",()=>discord.status());app.post("/api/discord/instances",async(req:any)=>{state.discord.push(req.body);await save();return state.discord});app.post("/api/discord/connect",async(req:any)=>discord.connect(req.body));app.post("/api/discord/:id/join",async(req:any)=>{await discord.join((req.params as {id:string}).id);return discord.status()});app.get("/api/discord/:id/guilds",req=>discord.guilds((req.params as {id:string}).id));app.get("/api/discord/:id/guilds/:gid/channels",req=>{const p=req.params as {id:string,gid:string};return discord.channels(p.id,p.gid)});
app.get("/api/ts3",()=>ts3.status());app.put("/api/ts3",async(req:any)=>{state.ts3=req.body?.instances||[];ts3.set(state.ts3);await save();return ts3.status()});app.post("/api/ts3/:id/connect",req=>ts3.connect((req.params as {id:string}).id));app.post("/api/ts3/:id/disconnect",async req=>{await ts3.disconnect((req.params as {id:string}).id);return ts3.status()});
app.get("/api/users",()=>state.users.map(u=>({id:u.id,name:u.name,role:u.role})));app.post("/api/users",async(req:any,rep)=>{if(!auth(req)?.role) return rep.code(403).send({error:"forbidden"});const b=req.body||{};state.users.push({id:randomUUID(),name:String(b.name),hash:hash(String(b.password)),role:b.role==="user"?"user":"admin"});await save();return{ok:true}});app.get("/api/files",async()=>{const dir=path.join(ROOT,"files");await mkdir(dir,{recursive:true});return (await import("node:fs/promises")).readdir(dir)});
app.get("/",(_,rep)=>rep.sendFile("index.html"));await app.listen({host:HOST,port:PORT});