import {EventEmitter} from 'node:events';
const commandNames=['play','pause','resume','skip','stop','clear','volume','queue','nowplaying','help'];
export const discordOpusBitrate=128_000;
export const discordPcmBufferBytes=384_000;
export const discordPendingPcmBytes=192_000;
export const discordPrebufferBytes=57_600;
export const discordReadyTimeoutMs=15_000;
function queueDiscordPcm(runtime,buffer){runtime.pendingPcm??=[];runtime.pendingPcmBytes??=0;while(runtime.pendingPcm.length&&runtime.pendingPcmBytes+buffer.length>discordPendingPcmBytes)runtime.pendingPcmBytes-=runtime.pendingPcm.shift().length;if(buffer.length<=discordPendingPcmBytes){runtime.pendingPcm.push(buffer);runtime.pendingPcmBytes+=buffer.length}}
export function flushDiscordPcm(runtime){const stream=runtime?.stream;if(!stream||stream.destroyed){runtime.pendingPcm=[];runtime.pendingPcmBytes=0;runtime.backpressured=false;return false}runtime.backpressured=false;while(runtime.pendingPcm?.length){const buffer=runtime.pendingPcm.shift();runtime.pendingPcmBytes-=buffer.length;if(!stream.write(buffer)){runtime.backpressured=true;break}}return !runtime.backpressured;}
export function writeDiscordPcm(runtime,buffer){if(!runtime?.stream||runtime.stream.destroyed)return false;if(runtime.started===false&&runtime.resource&&runtime.audio){runtime.backpressured=!runtime.stream.write(buffer);runtime.primedBytes=(runtime.primedBytes||0)+buffer.length;if(runtime.primedBytes>=discordPrebufferBytes){runtime.started=true;runtime.audio.play(runtime.resource);if(runtime.startPaused)runtime.audio.pause(true)}return !runtime.backpressured}if(runtime.backpressured){queueDiscordPcm(runtime,buffer);return false}runtime.backpressured=!runtime.stream.write(buffer);return !runtime.backpressured;}
export class IntegrationManager extends EventEmitter{
  constructor({player,store,secrets}){super();this.player=player;this.store=store;this.secrets=secrets;this.runtimes=new Map();this.connectionJobs=new Map();player.on('pcm',payload=>{if(payload?.buffer&&payload?.playerId){for(const[runtimeId,runtime]of this.runtimes)if(this.resolvePlayerId(runtimeId)===payload.playerId)runtime.write?.(payload.buffer);return}const {output,outputId}=store.data.settings;if(output==='none'||!outputId)return;const runtime=this.runtimes.get(outputId);if(runtime?.type===output)runtime.write?.(payload)});if(player.stateFor)player.on('instance-state',(id,state)=>this.syncPlayback(state,id));else player.on('state',state=>this.syncPlayback(state));}
  resolvePlayerId(id){return this.player?.resolve?.(id)||String(id||'')}
  playerFor(id){return this.player.for?.(this.resolvePlayerId(id))||this.player}
  playerStateFor(id){return this.player.stateFor?.(this.resolvePlayerId(id))||this.player.state()}
  syncPlayback(state,targetPlayerId=null){for(const[runtimeId,runtime]of this.runtimes){if(runtime.type!=='discord'||targetPlayerId&&this.resolvePlayerId(runtimeId)!==targetPlayerId)continue;try{const settings=this.store?.data?.settings,selected=targetPlayerId?this.resolvePlayerId(runtimeId)===targetPlayerId:!settings||settings.output==='discord'&&settings.outputId===runtimeId,presence=selected&&state.current&&!state.paused?String(state.current.title||'MusikBot187').slice(0,128):'';if(runtime.lastPresence!==presence&&runtime.client?.user){runtime.client.user.setPresence({activities:presence?[{name:presence,type:runtime.ActivityType.Listening}]:[],status:runtime.status==='online'?'online':'idle'});runtime.lastPresence=presence;}if(!selected||!state.current){if(runtime.playbackId!=null||runtime.lastPaused!=null)runtime.reset?.();runtime.lastPaused=null;continue;}runtime.startPaused=Boolean(state.paused);if(runtime.playbackId!==state.playbackId){runtime.prepare?.(state.playbackId);runtime.startPaused=Boolean(state.paused);runtime.lastPaused=null;}if(runtime.lastPaused!==state.paused){if(state.paused)runtime.audio.pause(true);else runtime.audio.unpause();runtime.lastPaused=state.paused;}}catch(error){runtime.status='error';runtime.detail=error.message;this.store?.diagnostic?.('error','discord',`${runtime.name||'Discord'}: Audioausgabe fehlgeschlagen: ${error.message}`).catch?.(()=>{});}}}
  status(){return [...this.runtimes.entries()].map(([id,r])=>({id,type:r.type,status:r.status||'offline',detail:r.detail||''}));}
  connect(connection){this.connectionJobs??=new Map();const running=this.connectionJobs.get(connection.id);if(running)return running;const operation=this.openConnection(connection),cleanup=()=>{if(this.connectionJobs.get(connection.id)===operation)this.connectionJobs.delete(connection.id)};this.connectionJobs.set(connection.id,operation);operation.then(cleanup,cleanup);return operation;}
  async openConnection(connection){await this.disconnect(connection.id,true);try{let runtime;if(connection.type==='discord')runtime=await this.connectDiscord(connection);else if(connection.type==='ts3')runtime=await this.connectTs3(connection);else throw new Error('Unbekannter Verbindungstyp');this.syncPlayback(this.playerStateFor(connection.id),this.resolvePlayerId(connection.id));return runtime;}catch(error){const failed=this.runtimes.get(connection.id);this.runtimes.delete(connection.id);try{await failed?.close?.()}catch{}this.emit('status',this.status());throw error;}}
  async connectDiscord(c){const {Client,GatewayIntentBits,REST,Routes,SlashCommandBuilder,ActivityType}=await import('discord.js');const {joinVoiceChannel,createAudioPlayer,createAudioResource,StreamType}=await import('@discordjs/voice');const {PassThrough}=await import('node:stream');const token=this.secrets.open(c.secret);if(!token)throw new Error('Bot-Token fehlt');const intents=[GatewayIntentBits.Guilds,GatewayIntentBits.GuildVoiceStates];if(c.messageContent)intents.push(GatewayIntentBits.GuildMessages,GatewayIntentBits.MessageContent);const client=new Client({intents}),audio=createAudioPlayer();const runtime={type:'discord',name:c.name,status:'connecting',manual:false,client,audio,ActivityType,joinVoiceChannel,stream:null,resource:null,started:false,primedBytes:0,startPaused:false,backpressured:false,pendingPcm:[],pendingPcmBytes:0,playbackId:null,lastPaused:null,lastPresence:null,prepare:playbackId=>{audio.stop(true);runtime.stream?.destroy();const stream=new PassThrough({highWaterMark:discordPcmBufferBytes}),resource=createAudioResource(stream,{inputType:StreamType.Raw});resource.encoder?.setBitrate(discordOpusBitrate);stream.on('drain',()=>{if(runtime.stream===stream)flushDiscordPcm(runtime)});runtime.stream=stream;runtime.resource=resource;runtime.started=false;runtime.primedBytes=0;runtime.backpressured=false;runtime.pendingPcm=[];runtime.pendingPcmBytes=0;runtime.playbackId=playbackId;},reset:()=>{audio.stop(true);runtime.stream?.destroy();runtime.stream=null;runtime.resource=null;runtime.started=false;runtime.primedBytes=0;runtime.startPaused=false;runtime.backpressured=false;runtime.pendingPcm=[];runtime.pendingPcmBytes=0;runtime.playbackId=null;runtime.lastPaused=null;},write:b=>writeDiscordPcm(runtime,b),close:async()=>{runtime.manual=true;runtime.voice?.destroy?.();runtime.reset();client.destroy();}};audio.on('error',error=>{runtime.status='error';runtime.detail=error.message;this.store.diagnostic('error','discord',`${c.name}: Audioausgabe fehlgeschlagen: ${error.message}`).catch(()=>{});});this.runtimes.set(c.id,runtime);this.syncPlayback(this.playerStateFor(c.id),this.resolvePlayerId(c.id));
    client.on('ready',()=>{void (async()=>{runtime.status='online';runtime.detail=client.user.tag;this.syncPlayback(this.playerStateFor(c.id),this.resolvePlayerId(c.id));const guild=client.guilds.cache.get(c.guildId);if(!guild){const visible=client.guilds.cache.map(item=>item.name).slice(0,5),message=visible.length?`Discord verbunden – bitte einen erreichbaren Server auswählen. Gefunden: ${visible.join(', ')}`:'Discord verbunden, aber der Bot wurde noch keinem Server hinzugefügt.';runtime.status='warning';runtime.detail=message;await this.store.diagnostic('warn','discord',`${c.name}: ${message}`);return}if(c.voiceChannelId)await this.joinDiscordVoice(c);else{runtime.status='warning';runtime.detail=`${client.user.tag} · kein Voice-Channel ausgewählt`;}const applicationId=client.application?.id||c.clientId;if(!applicationId)throw new Error('Discord Client-ID fehlt');const commands=commandNames.map(name=>new SlashCommandBuilder().setName(name).setDescription(`MusikBot187: ${name}`));commands[0].addStringOption(o=>o.setName('query').setDescription('Titel, Interpret oder URL').setRequired(true));commands[6].addIntegerOption(o=>o.setName('value').setDescription('0–100').setRequired(true).setMinValue(0).setMaxValue(100));await new REST({version:'10'}).setToken(token).put(Routes.applicationGuildCommands(applicationId,guild.id),{body:commands.map(x=>x.toJSON())});})().catch(error=>{runtime.status='error';runtime.detail=error.message;return this.store.diagnostic('error','discord',`${c.name}: ${error.message}`)}).catch(()=>{});});
    client.on('interactionCreate',i=>{if(!i.isChatInputCommand())return;void this.handleInteraction(i,c).catch(error=>this.store.diagnostic('error','discord',`${c.name}: Discord-Befehlsantwort fehlgeschlagen: ${error.message}`).catch(()=>{}));});
    client.on('messageCreate',m=>{if(!c.messageContent||!c.prefix||m.author.bot||m.guildId!==c.guildId||!m.content.startsWith(c.prefix))return;const [name,...rest]=m.content.slice(c.prefix.length).trim().split(/\s+/);if(!commandNames.includes(name))return;void this.handleCommand(name,rest.join(' '),Number(rest[0]),c.id).then(result=>m.reply(String(result).slice(0,1900))).catch(async error=>{try{await m.reply(String(error.message||error).slice(0,1900))}catch(replyError){await this.store.diagnostic('error','discord',`${c.name}: Präfix-Befehlsantwort fehlgeschlagen: ${replyError.message}`).catch(()=>{})}});});
    client.on('error',e=>this.store.diagnostic('error','discord',e.message).catch(()=>{}));const ready=new Promise((resolve,reject)=>{let timer;const cleanup=()=>{clearTimeout(timer);client.off('ready',onReady);client.off('error',onError)},onReady=()=>{cleanup();resolve()},onError=error=>{cleanup();reject(error)};timer=setTimeout(()=>onError(new Error('Discord-Anmeldung hat innerhalb von 15 Sekunden keinen Bereitschaftsstatus erreicht.')),discordReadyTimeoutMs);timer.unref?.();client.once('ready',onReady);client.once('error',onError)});await Promise.all([client.login(token),ready]);return runtime;}
  async handleInteraction(interaction,connection){if(interaction.guildId!==connection.guildId){await interaction.reply({content:'Dieser Bot-Befehl ist für diesen Discord-Server nicht freigeschaltet.',ephemeral:true});return}await interaction.deferReply();try{const result=await this.handleCommand(interaction.commandName,interaction.options.getString('query'),interaction.options.getInteger('value'),connection.id);await interaction.editReply(String(result).slice(0,1900));}catch(error){await interaction.editReply(`Fehler: ${String(error.message||error).slice(0,1880)}`);}}
  async handleCommand(name,query,value,playerId=null){const player=this.playerFor(playerId);if(name==='play'){const {youtubeSearch}=await import('./media.js');const [item]=await youtubeSearch(query);if(!item)throw new Error('Kein Treffer');player.add(item);return `Hinzugefügt: ${item.title}`;}if(name==='pause')player.pause();if(name==='resume')player.resume();if(name==='skip')player.skip();if(name==='stop')player.stop();if(name==='clear')player.clear();if(name==='volume')player.setVolume(value);if(name==='queue')return player.queue.map((x,i)=>`${i+1}. ${x.title}`).join('\n')||'Queue ist leer.';if(name==='nowplaying')return player.current?.title||'Nichts läuft.';if(name==='help')return commandNames.map(x=>`/${x}`).join(' · ');return 'OK';}
  async joinDiscordVoice(c,{connectIfNeeded=false}={}){let runtime=this.runtimes.get(c.id),client=runtime?.client;if((runtime?.type!=='discord'||!client?.isReady())&&connectIfNeeded){await this.connect(c);runtime=this.runtimes.get(c.id);client=runtime?.client}if(runtime?.type!=='discord'||!client?.isReady())throw new Error('Discord-Bot ist nicht verbunden. Bitte zuerst „Speichern / verbinden“ wählen.');if(!c.guildId)throw new Error('Discord-Server fehlt.');if(!c.voiceChannelId)throw new Error('Bitte zuerst einen Voice-Channel auswählen.');const guild=client.guilds.cache.get(c.guildId);if(!guild)throw new Error('Der ausgewählte Discord-Server ist für den Bot nicht erreichbar.');const voice=guild.channels.cache.get(c.voiceChannelId);if(!voice?.isVoiceBased?.())throw new Error('Der ausgewählte Voice-Channel wurde nicht gefunden oder ist kein Sprachkanal.');runtime.voice?.destroy?.();const connection=runtime.joinVoiceChannel({channelId:voice.id,guildId:guild.id,adapterCreator:guild.voiceAdapterCreator,selfDeaf:true});connection.subscribe(runtime.audio);runtime.voice=connection;runtime.status='online';runtime.detail=`${client.user?.tag||c.name} · Voice: ${voice.name}`;return {guildId:guild.id,channelId:voice.id,channelName:voice.name};}
  discordOptions(id){const runtime=this.runtimes.get(id),client=runtime?.client;if(!client?.isReady())return {status:runtime?.status||'offline',detail:runtime?.detail||'Discord-Bot ist nicht verbunden.',guilds:[],voiceChannels:[]};const visibleGuilds=[...client.guilds.cache.values()],guilds=visibleGuilds.map(guild=>({id:guild.id,name:guild.name})),voiceChannels=visibleGuilds.flatMap(guild=>[...guild.channels.cache.values()].filter(channel=>channel.isVoiceBased?.()).map(channel=>({id:channel.id,name:channel.name,guildId:guild.id,guildName:guild.name})));return {status:runtime.status||'online',detail:runtime.detail||'',guilds,voiceChannels};}
  async connectTs3(c){const {TeamSpeak}=await import('ts3-nodejs-library');const password=this.secrets.open(c.secret);const client=await TeamSpeak.connect({host:c.host,queryport:Number(c.port||10011),serverport:Number(c.serverPort||9987),username:c.username||'serveradmin',password,nickname:c.nickname||'MusikBot187'});const runtime={type:'ts3',name:c.name,status:'warning',detail:'ServerQuery verbunden · Audio-Transport benötigt einen echten TS3-Client',write:()=>{},close:()=>client.quit()};client.on('error',e=>this.store.diagnostic('error','ts3',e.message).catch(()=>{}));this.runtimes.set(c.id,runtime);return runtime;}
  async disconnect(id,remove=false){const r=this.runtimes.get(id);try{if(r)await r.close?.()}finally{this.runtimes.delete(id);if(remove)this.emit('status',this.status());}}
  async close(){const results=await Promise.allSettled([...this.runtimes.keys()].map(id=>this.disconnect(id)));const failed=results.find(result=>result.status==='rejected');if(failed)throw failed.reason;}
}
