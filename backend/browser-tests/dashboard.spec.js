import {test,expect} from '@playwright/test';
test('first-run setup, auth navigation and theme persistence',async({page})=>{await page.goto('/#setup=browser-setup-token');await expect(page.getByText('Ersteinrichtung')).toBeVisible();await page.getByLabel('Benutzername').fill('browseradmin');await page.getByLabel('Passwort').fill('browser-password-187');await page.getByRole('button',{name:'Einrichten'}).click();await expect(page.getByText('MusikBot187')).toBeVisible();await expect(page.getByRole('button',{name:'Player'})).toBeVisible();await page.getByRole('button',{name:'Design'}).click();await page.getByLabel('Theme').selectOption('ocean');await page.getByRole('button',{name:'Speichern'}).click();await expect(page.locator('body')).toHaveAttribute('data-theme','ocean');});
