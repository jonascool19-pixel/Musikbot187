import test from 'node:test';import assert from 'node:assert/strict';import {access} from 'node:fs/promises';test('Node runtime is supported',()=>assert.ok(Number(process.versions.node.split('.')[0])>=22));test('required project files exist',async()=>{for(const p of ['../backend/src/server.js','../frontend/index.html','../install-latest.sh'])await access(new URL(p,import.meta.url))});
