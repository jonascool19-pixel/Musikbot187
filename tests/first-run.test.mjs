import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs/promises';
test('installer and service contracts are complete',async()=>{const installer=await fs.readFile('../install-latest.sh','utf8');for(const value of ['NodeSource','sha256sum','musikbot187-control.service','npm ci','/var/lib/musikbot187','/api/health'])assert.match(installer,new RegExp(value.replaceAll('/','\\/'),'i'));const service=await fs.readFile('../systemd/musikbot187.service','utf8');for(const value of ['User=musikbot187','NoNewPrivileges=true','ProtectSystem=strict','ReadWritePaths='])assert.match(service,new RegExp(value));});
